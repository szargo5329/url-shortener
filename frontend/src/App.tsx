/**
 * Placeholder shell for the scaffold. The real UI (UrlForm, ResultCard) is built
 * in the next step; this only confirms the cyberpunk theme (fonts, palette, grid)
 * is wired up correctly.
 */
function App() {
  return (
    <main className="relative z-10 flex min-h-screen flex-col items-center justify-center gap-3 px-6 text-center">
      <p className="font-mono text-xs uppercase tracking-[0.2em] text-primary/60">
        // url compression utility
      </p>
      <h1 className="font-sans text-3xl font-bold tracking-tight text-foreground">
        SHR<span className="text-primary">.</span>T
      </h1>
      <p className="font-mono text-xs tracking-wider text-muted-foreground">
        frontend scaffold ready — components coming next
      </p>
    </main>
  )
}

export default App
