# Production builds call API Gateway directly. Served from the CloudFront origin,
# which is exactly what the backend's CORS policy allows.
VITE_API_BASE_URL=https://zhirkpafz2.execute-api.us-east-1.amazonaws.com
